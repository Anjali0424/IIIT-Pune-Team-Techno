fastapi>=0.115.0
uvicorn[standard]>=0.30.0
SQLAlchemy>=2.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Text-to-Speech (primary provider - Google Cloud TTS).
# Heavy dependency; the backend still starts without it and falls back to
# Azure, but install it to enable the best-quality Hindi/Marathi voices.
google-cloud-texttospeech>=2.14.0

# Text-to-Speech (fallback provider - Azure AI Speech REST calls).
httpx>=0.27.0

# Text-to-Speech (no-key fallback - Microsoft Edge neural voices).
# Works out of the box without any API keys; only needs an internet connection.
edge-tts>=6.1.3
